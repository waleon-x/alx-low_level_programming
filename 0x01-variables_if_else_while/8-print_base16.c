#include <stdio.h>
/**
 * main - Entry point
 * Return: 0 (Advance)
 */

int main(void)
{
	char w;

	int x;

	w = 'a';
	x = 0;
	while
		(x < 10) {
			putchar(x + '0');
			x++;
		}
	while
		(w <= 'f') {
			putchar(w);
			w++;
		}
	putchar('\n');
	return (0);
}
