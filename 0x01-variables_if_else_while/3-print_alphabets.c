#include <stdio.h>
/**
 * main - Entry point
 * Return: 0 (Advance)
 */

int main(void)
{
	char w;

	char x;

	w = 'a';
	x = 'A';
	while
		(w <= 'z') {
			putchar(w);
			w++;
		}
	while
		(x <= 'Z') {
			putchar (x);
			x++;
		}
	putchar('\n');
	return (0);
}
